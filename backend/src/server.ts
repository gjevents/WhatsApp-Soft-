// Legacy Node backend entry removed in favor of the Django API + independent WhatsApp service.
// This compatibility stub prevents stale imports from the previous architecture from showing as editor errors.
export {};